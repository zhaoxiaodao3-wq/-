# 销售记账前端接入指南

> 目标：把 `E:\code\sell-front\---`（sales-ledger 前端，React 18 + antd + Vite 5）
> 接入已部署的后端 API：**https://api.mia-fly.cn**（Nginx + Docker + SQLite）

---

## 0. 现状结论（先读这个）

**好消息：前端代码已经是"后端 API 对接版"，不需要改任何业务代码。**

- 所有请求统一走 `src/services/apiClient.js`：自动带 `Authorization: Bearer <token>`、401 自动清 token 并跳登录页
- 各业务 service（order/expense/payable/option/image/auth）已全部调用后端接口
- **你只需要正确设置 `VITE_API_BASE` 这一个配置，选一种部署方式即可**

当前配置（`E:\code\sell-front\---\.env`）：
```
VITE_API_BASE=/api
```

---

## 1. 方式 A：本地开发联调（最快上手）

前提：本地已启动后端（`cd E:\code\sell-server && node src/index.js`，端口 4000）。

```bash
cd E:/code/sell-front/---
npm install
npm run dev        # Vite 启动在 http://localhost:8000
```

`vite.config.js` 已配置代理：`/api → http://localhost:4000`，所以 `.env` 保持 `VITE_API_BASE=/api` 即可。

浏览器打开 `http://localhost:8000` → 用 `admin / admin` 登录。

---

## 2. 方式 B：生产部署（同域托管，推荐）

让后端直接托管前端静态文件（后端代码已预留此能力），访问 `https://api.mia-fly.cn/` 即前端，接口走同域 `/api`，**无跨域问题，最省事**。

```bash
cd E:/code/sell-front/---
npm install
npm run build                          # 产物在 dist/
```

然后把 `dist/` 里的文件传到服务器后端目录 `public/`：

```bash
# 本机执行（Windows 可用 scp，或 WinSCP 拖拽）
scp -r dist/* workuser@139.224.162.142:/home/workuser/sell-server/public/
```

最后重建容器（让新前端进镜像）：

```bash
# 服务器上执行
cd /home/workuser/sell-server
sudo docker compose up -d --build
```

完成后访问 **https://api.mia-fly.cn/** 即可看到前端页面。

> 注意：`public/` 在 `.gitignore` 里被忽略，但上传到服务器后 Docker 构建会 `COPY . .` 把它打进镜像，所以能正常生效。

---

## 3. 方式 C：生产部署（跨域直连后端）

如果你想把前端部署到**另一个站点/域名**（比如 `web.mia-fly.cn`），后端已开启 CORS（`app.use(cors())` 默认放行所有来源），直接跨域调用即可：

```bash
# Git Bash
VITE_API_BASE=https://api.mia-fly.cn/api npm run build

# PowerShell（Windows）
$env:VITE_API_BASE="https://api.mia-fly.cn/api"; npm run build
```

构建产物 `dist/` 部署到任意静态服务器（宝塔新建站点 / OSS / Nginx 等）即可。

> ✅ **已按此方案部署完成（2026-08-02）**：前端已构建并发布到 **https://web.mia-fly.cn/**（服务器站点目录 `/www/wwwroot/web.mia-fly.cn/`），API 指向 `https://api.mia-fly.cn/api`，跨域 CORS、静态资源、登录均已验证通过。旧版首页备份为 `index.html.bak`。
> 部署方式：本机 `VITE_API_BASE=https://api.mia-fly.cn/api npm run build` → dist 上传覆盖站点目录（保留 `.well-known`）→ 无需重启任何服务。

---

## 4. 鉴权机制（前端已自动处理，仅说明）

| 环节 | 说明 |
|---|---|
| 登录 | `POST /api/auth/login`，body `{"username":"admin","password":"admin"}` |
| 响应 | `{ token, username }`，token 为 JWT（有效期 30 天） |
| 存储 | 前端存入 `localStorage['sl_auth']` |
| 请求头 | 前端自动加 `Authorization: Bearer <token>` |
| 失效处理 | 接口返回 401 → 自动清 token → 跳转 `/login` |

默认账号 **admin / admin**（上线后建议改密码）。

---

## 5. 接口清单（前端 service ↔ 后端接口）

| 前端 service 文件 | 接口 | 方法 | 鉴权 |
|---|---|---|---|
| `services/authService.js` | `/auth/login` | POST | 公开 |
| `services/orderService.js` | `/orders`、`/orders/:id` | GET/POST/PUT/DELETE | 需 token |
| `services/expenseService.js` | `/expenses`、`/expenses/:id` | GET/POST/PUT/DELETE | 需 token |
| `services/payableService.js` | `/payables`、`/payables/:id`、`/payables/:id/settle` | GET/POST/PUT/DELETE/POST | 需 token |
| `services/optionService.js` | `/options`、`/options/custom`、`/options/order-title-history`、`/options/expense-title-history` | GET/POST | 需 token |
| 统计（页面内计算或后端） | `/stats` | GET | 需 token |
| `services/imageService.js` | `/images`（上传）、`/images/:key`（读取/删除） | POST/GET/DELETE | 读取公开，上传/删除前端会带 token |

> 所有路径前面都自动拼接 `VITE_API_BASE`。例如 `VITE_API_BASE=https://api.mia-fly.cn/api` 时，订单列表实际请求 `https://api.mia-fly.cn/api/orders`。

---

## 6. 图片上传 / 显示

- **上传**：`POST /api/images`，FormData 字段名 `file`（multipart），限制 **5MB**、仅图片类型
- **显示**：`imageService.getImageURL(key)` 返回 `${BASE}/images/${key}`，`<img>` 直接用即可（读取接口公开，无需 token）
- **删除**：`DELETE /api/images/:key`
- 后端 Nginx 已配 `client_max_body_size 50m`，前端上传 5MB 以内没问题

---

## 7. 常见问题

| 现象 | 原因 / 解决 |
|---|---|
| 登录后页面报错 | 确认 `VITE_API_BASE` 正确；F12 Network 看请求 URL 是否为 `.../api/...` |
| 跨域报错（方式 C） | 后端已 `cors()` 全放开；若仍报错，确认访问的是 `https://api.mia-fly.cn` 而非 `http://` |
| 401 跳登录页 | token 过期（30 天）或未登录，重新登录即可 |
| 图片上传失败 | 检查文件是否 >5MB 或非图片格式 |
| 上传大 JSON 失败 | 后端 `express.json` 限制 10MB，一般不会触发 |

---

## 8. 验收清单（接入完成后逐项勾选）

- [ ] 打开页面出现登录页，用 `admin/admin` 能登录成功
- [ ] 订单模块：能新增订单、列表显示、编辑、删除
- [ ] 支出模块：能新增/编辑/删除支出
- [ ] 应付模块：能新增应付、记录结清流水
- [ ] 选项设置：能添加自定义收款方式/渠道，刷新后保留
- [ ] 图片：上传凭证图片后能正常显示
- [ ] 刷新页面：仍保持登录状态（token 在 localStorage）
- [ ] F12 → Network：所有 `/api/...` 请求都带 `Authorization: Bearer xxx` 头
- [ ] 数据已存入服务器 SQLite：`/home/workuser/sell-server/data/db.sqlite`

---

## 附：后端接口自测命令（验证后端可用）

```bash
curl https://api.mia-fly.cn/health
curl -X POST https://api.mia-fly.cn/api/auth/login -H "Content-Type: application/json" -d "{\"username\":\"admin\",\"password\":\"admin\"}"
```
